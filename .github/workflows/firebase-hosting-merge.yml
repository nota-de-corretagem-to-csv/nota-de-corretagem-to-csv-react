# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install --frozen-lockfile &&  PUBLIC_URL='.' REACT_APP_API_URL=https://us-central1-nota-de-corretagem-to-csv.cloudfunctions.net/cloud_function yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_NOTA_DE_CORRETAGEM_TO_CSV }}'
          channelId: live
          projectId: nota-de-corretagem-to-csv
